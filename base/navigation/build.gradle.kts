plugins {
    id("kotlin.library.convention.plugin")
    id("compose.convention.plugin")
    id("kotlinx.convention.plugin")
}

kotlin {
    android {
        namespace = "nz.co.warehouseandroidtest.navigation"
    }
}
